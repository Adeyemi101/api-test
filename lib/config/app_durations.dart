part of 'config.dart';

class AppDurations {
  static const Duration animationDuration = Duration(milliseconds: 200);
  static const Duration popOutDuration = Duration(milliseconds: 400);
  static const Duration tooltipDuration = Duration(milliseconds: 900);
}
